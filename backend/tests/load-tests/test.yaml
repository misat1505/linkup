config:
  target: "https://localhost:5500"
  phases:
    - duration: 60
      arrivalRate: 10
  tls:
    rejectUnauthorized: false
scenarios:
  - flow:
      - post:
          url: "/auth/login"
          json:
            login: "login1"
            password: "pass1"
